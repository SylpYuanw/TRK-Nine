# 死之弓:实现与依据

## 结论

- 武器为单实例 + 内部状态:模式(普通/闪弓)、蓄力阶(1~4)、剩余次数(0~4)全部保存在武器组件里,不生成第二件物品,也不替换贴图。
- 远程射击走**武器自身的射击动词**,因此命中率、掩体、天气、射手精度、战斗日志、鼠标悬停命中率提示全部是原版链路;蓄力阶的精度加成通过武器精度统计叠加,命中判定直接读取。
- 次数与弹药:闪弓每射一次消耗 1 次,次数由"死之箭"从背包装填补满;次数为 0 时无法远程射击,需手动切回普通模式,只保留近战。

## 新增文件

- `Defs/ThingDefs_Items/Weapon_BowOfDeath.xml`:死之弓本体(近战弓臂 + 闪弓动词 + 组件数据)。
- `Defs/ThingDefs_Items/Ammo_BowOfDeath.xml`:死之箭物品。
- `Defs/Projectiles/Projectiles_BowOfDeath.xml`:四阶弹丸。
- `Defs/HediffDefs/Hediffs_BowOfDeath.xml`:箭矢(承伤 +14%/层)与意识提升(4 小时)。
- `Defs/RecipeDefs/Recipes_BowOfDeath.xml`:死之箭制作(25 钢铁,机械加工台/锻造台)与手术"取出箭矢"。
- `Source/ClassLibrary1/Weapon/BowOfDeath/*.cs`:组件、射击动词、弹丸、DefOf(与派送箱代码互不引用)。
- `Defs/Misc/Mote_BowOfDeath.xml`:蓄力阶切换浮现印迹与四阶蓄力三层瞄准特效的 Mote 定义(印迹用绑定角色的原版 MoteAttached)。
- `Textures/VFX/Bow_of_Death_Stage_Vfx.png`:浮现印迹贴图(原文件名 `B2702C632A3C2E434244095E08C7596F.png` 已改名)。
- `Textures/VFX/Bow_of_Death_Aim_Vfx.png`:蓄力瞄准烟雾贴图(原文件名 `ChatGPT Image 2026年9月19日 14_39_40.png` 已改名,1254x1254,素材自身不透明度约 81%~88%)。
- `Source/ClassLibrary1/Weapon/BowOfDeath/Mote_DeathBowAimVfx.cs`:蓄力瞄准特效的 Mote 基类与三层子类(透明度上限、呼吸、摇曳,以及流线层的瞄准方向定向)。
- `Textures/VFX/Bow_of_Death_Aim_Base_Vfx.png`:蓄力特效基底贴图(原文件名 `ChatGPT Image 2026年9月19日 15_21_46.png` 已改名,1514x1039,升腾火苗/烟柱)。
- `Textures/VFX/Bow_of_Death_Aim_Streak_Vfx.png`:四阶蓄力流线层贴图(原文件名 `9A813A8BDFC89E68CAB4DF9D3134B2B7.png` 已改名,256x256,横向红色流光;可见内容 x 5~255、y 114~147 像素)。
- `Source/自检_引用一致性.ps1`:DefOf 字段与 Defs 定义、XML 与程序集类型的引用一致性自检脚本。
- `Defs/SoundDefs/Sounds_BowOfDeath.xml`:拉弓 Sustainer、射击单发、蓄力阶切换三个 SoundDef。
- `Sounds/Bow_of_Death_Draw.wav` / `Bow_of_Death_Shot.wav` / `Bow_of_Death_Switch.wav`:素材改名自 `Eshifaust_4_1-4拉弓.ogg.wav` / `-5射击.ogg.wav` / `-1切换.ogg.wav`(原文件实为 16bit PCM 44.1kHz 立体声 WAV,双重扩展名去掉)。
- `Defs/ShaderTypeDefs/BowOfDeath_Shaders.xml`:两个自绘 Shader 的 `ShaderTypeDef`(`shaderPath` = `Trk/BowOfDeathAimBase` / `Trk/BowOfDeathAimWind`)。
- `AssetBundles/trk_bowofdeath_win`(+`.manifest`):随 MOD 分发的 Windows Shader 包,包内资源为 `Assets/Data/Trk.Nine/Materials/Trk/BowOfDeathAimBase.shader` 与 `...AimWind.shader`;源工程在 `D:\UnityTest\unity_trk_35`。
- `Languages/.../Keyed/BowOfDeath.xml`:按钮与提示文案。

## 机制口径

- 武器分类:带远程动词 → `IsRangedWeapon=true`,与狙击步枪同型(远程为主 + 保留近战 tool)、近战走原版 tool+机动。
- 射击完全走原版攻击命令:闪弓模式下射击动词使用原版参数(`isPrimary=true`、`hasStandardCommand=true`),装备按钮栏出现原版远程攻击命令(快捷键同样走原版绑定);普通模式或死之箭耗尽时,该动词被换成同实例的休眠参数(`isPrimary/hasStandardCommand/targetable` 全关),武器只剩近战动词与近战命令。参数切换只改这三个布尔,"弹丸/射程/瞄准时间"字段保持一致,且只在非预热、非爆发时执行。
- 精度:`statBases` 四段基础精度 64/74/84/74;每阶在武器统计上叠加 +4%/8%/12%/16%(写入 XML 的 `accuracyOffset`,可随时改)。
- 瞄准时间:四阶各自的总值 4 / 5.4 / 6.8 / 8.2 秒(基础 4 秒 + 该阶增量 0/1.4/2.8/4.2),由射击动词的 `WarmupTime` 提供。
- 弹丸:伤害 24/34/47/66(33.6、47.04 取整),穿透 0.34/0.476/0.6664/0.933,速度 44.4,抑止 4;伤害类型沿用原版 `Arrow`。
- 箭矢层数:命中血肉单位后挂 1 层,同一单位最多 4 层;每层 `IncomingDamageFactor +0.14`;不可自然愈合、不可包扎,一次手术清除全部层数(与豪猪棘刺同结构)。
- 意识增益:每次射击完成后给射手刷新 4 小时(10000 tick)的意识 +14%,重复射击只刷新不叠加。

## 蓄力阶切换特效(浮现印迹)

**结论**:蓄力阶切换完成时(与切换音效同一时点),在装备者身上挂一枚绑定角色的 Mote,表现"淡入 → 保持 → 上浮回落 → 淡出",约 1.18 秒后由原版机制自行销毁。连续切换不叠图:上一枚仍在播放且宿主未变时,只把它的时间轴推回起点重播。

**为什么走 Mote 而不是自绘**:装备中的武器不是地图上的已生成物(`Thing.Spawned=false`),`ThingComp.PostDraw` 不会被调用,所以不能像 Milira 的棋子浮标那样在武器组件里 `Graphics.DrawMesh`。Mote 是地图上的真实 Thing,由地图动态绘制,天然满足"跟随角色、超时自毁、不入存档"三个要求,且不需要任何 Harmony 补丁或每帧热路径。

**实现分层**:

- XML(`Defs/Misc/Mote_BowOfDeath.xml`):`ParentName="MoteBase"` + `<thingClass>MoteAttached</thingClass>`;`<mote>` 段给 `fadeInTime 0.08 / solidTime 0.6 / fadeOutTime 0.5`(三段之和即 Lifespan 1.18)、`attachedDrawOffset (0,0,0.6)`(画面上下方向的头顶偏移)、`archHeight 0.35 / archDuration 1.18`(显示期间的上浮弧线);`graphicData` 指向 `VFX/Bow_of_Death_Stage_Vfx`,`shaderType MoteGlow`,`drawSize 1.5`。
- C#(`CompEquippable_DeathBow.PlayStageSwitchVfx`):取 `EquippedPawn` → 已有存活印迹且宿主相同则 `ForceSpawnTick(当前 tick)` 重播,宿主已变则 `Destroy()` 旧实例,否则 `MoteMaker.MakeAttachedOverlay(pawn, DeathBowDefOf.BowOfDeath_StageVfx, Vector3.zero)` 新起一枚。
- 触发点:`RequestStageSwitch` 的立即切换分支与 `ApplyPendingIfReady` 的延迟完成分支各调用一次 `PlayStageSwitchVfx()`,因此 0 tick 与带等待时间两种配置都在"该阶生效的同一帧"出特效,与 `<switchSound>` 完全同步;近战/闪弓模式切换不出该特效。

**参数口径**:改观感只动 XML —— 时长改 `<mote>` 三段(同时把 `archDuration` 调到不超过新的三段和),大小改 `drawSize`,位置改 `attachedDrawOffset`,浮起幅度改 `archHeight`;`stageSwitchVfx` 是运行时字段,不写入存档(mote 本身 `isSaveable=false`),读档后自然为空,下次切换重新生成。

## 蓄力瞄准特效(三层叠加)

**本轮改造(Shader 化)**:两层烟雾画面不再由贴图本身决定,改由各自的自绘 Shader 在**一次 DrawMesh 内程序化生成**;贴图退化为"火焰形体来源"与"烟雾软层"。C# 侧只保留层呼吸、呼吸缩放与摇曳,不再承担画面生成。第三层流线层不参与这套自绘 Shader:它用原版发光 Mote 着色器绘制贴图本身,只有朝向与位移由 C# 按角色面朝方向计算。

| 层 | Shader(`ShaderTypeDef`) | 画面内容 | 贴图用途 |
| --- | --- | --- | --- |
| 侧面层 `BowOfDeath_AimVfx` | `Trk/BowOfDeathAimWind`(`Trk_BowOfDeathAimWind`) | 主体是**贴图本身**:不裁剪、不扭曲,按贴图自带颜色显示,随 `_SmokeScroll` 整图向左平移(取样读数增大 → 画面内容左移),循环接缝由贴图自身的透明边缘隐藏;辅助拖尾粒子在右侧发射带出生,沿 +20°(世界 -X 略偏 +Z)漂移后淡出,头部只留轻微红色鼓包 | 画面主体(形状、透明度与颜色都来自贴图;`_SmokeStrength` 设 0 即只剩辅助粒子) |
| 基底层 `BowOfDeath_AimBaseVfx` | `Trk/BowOfDeathAimBase`(`Trk_BowOfDeathAimBase`) | 类高斯光团(径向亮度 `exp(-(r/σ)^n)`,单调不增、无台阶) + 贴图火苗的双相位向上输送、噪声侵蚀与明暗闪烁 | 火焰形体来源(贴图 alpha 经 `_FlameCut/_FlameSoft` 剔除 1~7/255 底噪) |
| 流线层 `BowOfDeath_AimStreakVfx` | 原版 `Map/MoteGlow`(`shaderType MoteGlow`) | 贴图本身:一条横向红色流光,不裁剪、不扭曲,按贴图自带颜色显示,长轴随角色面朝方向(取值 `面朝角 - 90`) | 画面全部(贴图 alpha 直接作为透明度,`drawSize (4,2)` 只做缩放) |

**Shader 从哪来**:RimWorld 只从 AssetBundle 或 Resources 取 Shader(`Verse/ShaderDatabase.cs:TryLoadShader` → `Verse/ContentFinder.cs:TryFindAssetInModBundles`),取不到就 `Log.Warning` 并回退默认 Shader(`Map/Cutout`)。因此新增 `Defs/ShaderTypeDefs/BowOfDeath_Shaders.xml` 与 `AssetBundles/trk_bowofdeath_win`。包内资源路径必须是 `Assets/Data/<packageId>/Materials/<shaderPath>.shader`(本 MOD `About.xml` 的 packageId 为 `Trk.Nine`),包文件名必须以 `_win` 结尾,且**扩展名必须为空**。

**参数契约**:Shader 属性 = XML `shaderParameters` ∪ 4 个内部属性(`_MainTex`、`_Color`、`_PreviewClockEnabled`、`_PreviewSeconds`,只供编辑器预览,禁止写入 XML)。基底层 16 个、侧面层 19 个 XML 参数;改观感只改 XML,不用重打包。每帧唯一的实例级数据是 `_Color`(`Graphic_Mote.DrawMote` 写入),逐粒子与逐帧变化必须在 Shader 内用 `_Time` 生成。

**Unity 侧证据(GPU 实测,`unity/死之弓蓄力Shader_20260919/GPU验收/`)**:基底层 ±X 高斯拟合 σ=0.625 格、R²=0.9999,±Z σ=0.332 格、R²=0.9999,剖面单调不增;火焰区域帧间 Alpha 变化 40.8%(要求 >40%);6 帧合成图空白帧 0。其中"侧面层单粒子质心 ΔX=-0.382 格/0.12 秒、角度 22°"一组数值对应**初版粒子口径**;2026-09-19 追加修订后侧面层已改为"贴图烟雾为主体、粒子降为辅助拖尾",观感改由用户在 Unity 预览窗口与游戏内验收。

**Shader 侧调参入口**:参数预览与批量导出用工程菜单"死之弓/预览窗口";改动 Shader 源码后跑 `BowOfDeathBundleBuilder.BuildAndDeploy` 重新打包并复制进 MOD(`ChunkBasedCompression | StrictMode | ForceRebuildAssetBundle`),打包流程不写入仓库。

**结论**:**仅四阶蓄力**会出瞄准特效(阶 Def 的 `aimVfx` 开关决定,当前只有四阶为 true,其余三阶正常瞄准但不冒烟)。特效由**三层 Mote 叠加**构成,一起挂在装备者身上,不做喷发式堆叠;前两层是固定朝向的烟雾,第三层随角色面朝方向定向:

| 层 | 贴图 | 透明度行为 | 图层 |
| --- | --- | --- | --- |
| 主体层 `BowOfDeath_AimVfx` | `VFX/Bow_of_Death_Aim_Vfx`(圆烟) | 上限 70%;蓄力期间**反复淡出**(最低 8%、周期 1.7 秒)形成"一呼一吸"的强节拍;射击完成后走最终 1 秒淡出 | `MoteOverhead` |
| 基底层 `BowOfDeath_AimBaseVfx` | `VFX/Bow_of_Death_Aim_Base_Vfx`(升腾火苗) | 上限 85%;**常驻不消失**,只做最低 55%、周期 2.6 秒的轻呼吸 | `MoteOverheadLow`(垫在主体层下面) |
| 流线层 `BowOfDeath_AimStreakVfx` | `VFX/Bow_of_Death_Aim_Streak_Vfx`(横向流光) | 上限 80%;瞄准期间**不呼吸、不反复淡出**,只做 1 秒淡入 / 1 秒淡出 | `VisEffects`(压在两层烟雾之上) |

透明度最终值 = 原版淡入淡出曲线(`Mote.Alpha`)× 上限(`Opacity`)× 反复呼吸曲线(`Pulse`),三者在 `Mote_DeathBowAimVfxBase.Alpha` 里相乘;`Graphic_Mote.DrawMote` 会把它乘进顶点色。流线层的最低呼吸值与上限相同,`Pulse` 恒为 1,因此它的透明度只走原版淡入淡出曲线 × 0.8。

**触发与生命周期的分工**(与早期"固定地点呼吸感"版一致):

- 开始:`Verb_DeathBowShot.TryStartCastOn` 覆写调用原版逻辑后,若已进入预热状态就通知 `CompEquippable_DeathBow.Notify_AimStarted()`。该方法先判断 `CurrentStage?.aimVfx == true`,再对三层各调用一次 `EnsureAimVfx`(已有存活实例且宿主相同时复用,否则重建)。这是原版建立 `Stance_Warmup` 的唯一入口,近战动词不经过这里。
- 续命:每个 Mote 每 tick 自查"装备者是否仍在这把弓的预热状态"(`pawn.stances.curStance as Stance_Warmup` 且 `verb is Verb_DeathBowShot` 且 `WarmingUp`),成立则调用 `Maintain()`。
- 结束:停止 `Maintain()` 后,原版 `Mote.Alpha` 按 `fadeOutUnmaintained` 在 `fadeOutTime`(1 秒)内线性降到 0,`Mote.TimeInterval` 在同样时长后 `Destroy()`;"射击完成""瞄准被打断""装备者死亡/离图"都走这一条,不需要额外计时器。
- 读档:Mote 不入存档,`PostExposeData` 的 `PostLoadInit` 阶段若这把弓正在预热,补挂一次。

**共用动效(两层烟雾)**(`phase` 各自随机,多把弓同时蓄力不会整齐划一):

| 效果 | 实现 | 取值 |
| --- | --- | --- |
| 呼吸缩放 | `TimeInterval` 里改写 `linearScale` | 振幅 5%、频率 1.05 |
| 摇曳位移 | 覆写 `DrawPos` 叠加两条正弦位移 | x 0.06 / z 0.04 格,频率 0.72 / 0.47(带 1.7 相位差) |
| 轻微旋转 | 直接写 `exactRotation` | ±4 度 |
| 反复淡出 | `Alpha` 里乘 `Pulse` | 主体层 8%~100%、周期 1.7 秒;基底层 55%~100%、周期 2.6 秒 |

流线层不参与上表:它随角色面朝方向定向,因此关掉摇曳位移(位移改为沿面朝方向前移)、旋转摇曳(朝向改为面朝角 - 90)与呼吸缩放(振幅 0),反复呼吸也设为恒定(=不淡出),只保留 1 秒淡入 / 1 秒淡出。

**易错点**:

- `Mote.ExactScale` 不是 virtual,不能覆写;`Graphic_Mote` 实际读的是 `DrawPos / Alpha / exactRotation / ExactScale`,自绘动效要改这几个成员(缩放必须改写 `linearScale`)。
- `needsMaintenance` 的 Mote 必须在首个 tick 就 `Maintain()`,否则 `lastMaintainTick` 仍是 0,原版会立刻判定超时并销毁。
- 三层要用不同 `altitudeLayer`(`MoteOverheadLow` / `MoteOverhead` / `VisEffects`),否则同层同位置会出现深度打架。
- 反复淡出的最低不透明度不要取 0:`Graphic_Mote.DrawMote` 在 `alpha <= 0` 时整帧跳过绘制,观感会变成"闪断"而不是"淡出"。
- 朝向要减 90°:`Mote` 与武器贴图都是"用 `Quaternion.AngleAxis(角度, Vector3.up)` 画一张平面",原版 `PawnRenderUtility.DrawEquipmentAiming` 给武器取的是 `瞄准角 - 90 + equippedAngleOffset`,所以让贴图长轴指向面朝方向的取值是 **`面朝角 - 90`**(`AngleFlat` 口径:0=北、90=东、180=南、270=西)。直接用 `AngleFlat()` 原值会正好差 90°(实测表现为弓平着、线竖着)。
- 不要用 `Stance_Warmup` 给 `aimingChargeMote` 写的 `AimDir().AngleFlat()` 去推断贴图长轴的朝向口径:那类原版瞄准光效是对称光团,转与不转看不出差别,不能作为平面贴图坐标轴的依据。
- 流线层的朝向与位置每 tick 从预热状态重取(`Stance_Warmup.focusTarg`,与原版武器绘制同一份数据),目标移动时跟随;取不到状态(瞄准已结束进入淡出、或目标与装备者几乎重合)时退回角色当前朝向 `pawn.Rotation.AsAngle`,`aimAngle` 不会跳变。
- 三层子类只改 `Opacity / PulseMinAlpha / PulsePeriodSeconds` 三个参数;流线层另外覆写 `RotationAt`(朝向)、`OffsetAt`(位移)与 `BreathScaleAmplitude`(尺寸呼吸),动画与生命周期代码全部继承自 `Mote_DeathBowAimVfxBase`,换层不用复制代码。

**调参入口**:主体层节奏改 `Mote_DeathBowAimVfx` 的三个常量,基底层改 `Mote_DeathBowAimBaseVfx`;流线层长度改 Def 的 `drawSize.x`(当前 4 格),沿面朝方向的前移量改 `Mote_DeathBowAimStreakVfx.WeaponDrawDistance`(当前 0.4 格,与原版武器绘制同值,武器 Def 的 `equippedDistanceOffset` 会叠加),透明度上限改该类的 `Opacity`;贴图、尺寸、淡入淡出时长都在 `Defs/Misc/Mote_BowOfDeath.xml` 的三个瞄准 Def 里;要再加一层,照抄一个 Def + 在 `Notify_AimStarted` 里加一行 `EnsureAimVfx`。

## 音效接线(拉弓 / 射击 / 蓄力阶切换)

**结论**:四条音效全部走原版现成钩子或组件内既有的装备 Gizmo,不新增每帧逻辑 —— 拉弓用 `VerbProperties.soundAiming`(由 Sustainer 承载,但素材只播放一次,不循环),射击用 `VerbProperties.soundCast`(单发),蓄力阶切换与模式切换沿用组件里既有的 `PlaySwitchSound`(每次切换各播一次),装填在独立 Gizmo 内用自己的播放分支。四阶切换目前共用 `BowOfDeath_StageSwitch`,要分阶细分只改 `Weapon_BowOfDeath.xml` 里对应阶的 `<switchSound>`。

| 用途 | SoundDef | 触发点(源码) | 播放方式 |
| --- | --- | --- | --- |
| 瞄准(拉弓) | `BowOfDeath_Aim` | `Verse/Stance_Warmup.cs:69` `soundAiming.TrySpawnSustainer(SoundInfo.InMap(caster, MaintenanceType.PerTick))` | Sustainer 承载,素材只播放一次;随瞄准结束自动淡出收尾 |
| 射击 | `BowOfDeath_Shot` | `Verse/Verb.cs:468` `soundCast.PlayOneShot(new TargetInfo(caster.Position, caster.MapHeld))`(在 `TryCastNextBurstShot` 内,`TryCastShot()` 成功后) | 单发,按地图位置衰减 |
| 蓄力阶切换 | `BowOfDeath_StageSwitch` | `XIYUNTE.CompEquippable_DeathBow.PlaySwitchSound`,在阶生效瞬间(立即切换分支与延迟完成分支各一处) | 单发,`maxSimultaneous=1` 连点不叠音 |
| 装填死之箭 | `Bow_Large`(原版巨弓音效) | `XIYUNTE.CompEquippable_DeathBow.MakeLoadGizmo` 的按钮动作,`LoadAmmoFromInventory` 成功(loaded > 0)之后 | 单发,直接在装填 Gizmo 内按装备者位置播放,不经过切换音效入口;装填失败只出拒绝提示 |

**Sustainer 的两条硬性要求**(原版 `SoundDef.ConfigErrors` 会报错):`sustain=true`;`priorityMode` 不能是 `PrioritizeNewest`,故取 `PrioritizeNearest`(`maxSimultaneous=2` 也只对 sustainer 生效)。结束机制:`Sustainer.SustainerUpdate` 中 `MaintenanceType.PerTick` 若超过 1 tick 未被 `Maintain()` 就 `End()`,再过 `sustainFadeoutTime` 秒清理;`Stance_Warmup.StanceTick` 每 tick 只负责 `Maintain()`,所以"瞄准被打断/结束"时声音自然停止,不需要手写停音。

**"只播放一次"的实现依据**:`soundAiming` 只能走 Sustainer(原版没有"瞄准开始时播一次单发音"的字段),所以用两个 SubSoundDef 字段把它压成一次性:① `sustainLoop=false` —— `SampleSustainer.TryMakeAndPlay` 只在 `sustainLoop=true` 时给 AudioSource 置 `loop=true`,关掉后素材播完即止;② `sustainIntervalRange=9999~9999` —— `SubSustainer.StartSample` 用 `nextSampleStartTime = 样本结束时刻 + sustainIntervalRange` 排下一次取样,值远超最长蓄力(8.2 秒)即整个瞄准期不会再起第二个样本。素材时长 2.18 秒 < 最短蓄力 4 秒,所以不存在被提前掐断的情况;若以后出现短于素材的蓄力,结束时会按 `sustainFadeoutTime` 淡出。

**混音口径(素材实测)**:三份素材实测 RMS 拉弓 -24.1 dBFS、射击 -18.1 dBFS、切换 -34.1 dBFS(切换素材首 0.25 秒约 -28 dB,后段长衰减到 -90 dB)。播放增益 = `volumeRange.RandomInRange / 100`(`SubSoundDef.RandomizedVolume`),因此切换素材故意给了 95~105 来补偿其本身偏低的电平;三处 volumeRange 就是以后调平衡的唯一入口。瞄准音用 `tempoAffectedByGameSpeed` 让素材播放速度跟随游戏速度,`muteWhenPaused` 保证暂停时不响。

**未改动项**:武器 `soundInteract`(拾取/交互音)仍是原版 `Bow_Large`;近战/闪弓模式切换音 `modeSwitchSound` 仍是占位 `Interact_PlasmaSword`。两者都不在本轮需求内。

## 证据来源(RIMSAGE 1.6)

- `Verse/ShotReport.cs`:`HitReportFor` 组成;`HitFactorFromShooter` 用 `ShootingAccuracyPawn^距离`。
- `Verse/VerbProperties.cs:650 GetHitChanceFactor`:装备存在时读装备的 `AccuracyTouch/Short/Medium/Long`,并按 3/12/25/40 分距离插值后钳到 0.01~1。
- `RimWorld/StatWorker.cs:354`:Thing 自身统计会累加各组件 `GetStatOffset` → 武器组件的精度加成进入武器统计。
- `Verse/Verb_LaunchProjectile.cs`、`Verse/Verb_Shoot.cs`:射击、脱靶、掩体判定链;`TryCastShot` 返回后扣次数。
- `Verse/CompEquippable.cs:66` + `Verse/Pawn_EquipmentTracker.cs:235`:装备 Gizmo 入口是 `CompEquippable.CompGetEquippedGizmosExtra` → 本武器组件直接继承 `CompEquippable`。
- `RimWorld/CompPorcupine.cs` + `Hediff_PorcupineQuill` + `RemovePorcupineQuill`:命中后挂 Hediff、只能手术取出的原版模板;`Recipe_RemoveHediff` 在 `targetsBodyPart=false` 时清除全部同类 Hediff。
- `Verse/DamageWorker_AddInjury.cs:151`:承伤系数 `IncomingDamageFactor` 的生效点;`Verse/GenDate.cs`:1 小时 = 2500 tick。
- `Verse/RecipeDef.cs`:配方用 `recipeUsers` 直接挂到工作台。
- `Verse/Mote.cs`:`Attach`、`ForceSpawnTick`、`Alpha`(fadeInTime/solidTime/fadeOutTime 三段)、`EndOfLife`、`DrawPos`(archHeight/archDuration 生成上浮弧线)、`TimeInterval`(超时 `Destroy`)。
- `Verse/MoteAttached.cs`:`TimeInterval` 每 tick 用 `link1.UpdateDrawPos()` + `attachedDrawOffset` 把位置同步到宿主,越界销毁。
- `RimWorld/MoteMaker.cs:92 MakeAttachedOverlay`:`MakeThing` → `Attach(thing, offset)` → `Spawn` 三件套。
- `Verse/MoteAttachLink.cs`:`Linked/Target/UpdateDrawPos`;`LastDrawPos = Thing.SpawnedParentOrMe.DrawPos + 偏移`。
- `Verse/MoteProperties.cs`:三段时长与 `Lifespan = fadeInTime + solidTime + fadeOutTime`;`Defs/Core/Effects/Mote_Visual.xml:6 MoteBase`:`category=Mote`、`Graphic_Mote`、`tickerType=Normal`、`isSaveable=false`。
- `Verse/Stance_Warmup.cs:69`(soundAiming → `TrySpawnSustainer`)、`:162`(`sustainer.Maintain()`);`Verse/Sound/Sustainer.cs:SustainerUpdate`(PerTick 未维持即 `End`)、`Maintain`;`Verse/Sound/SubSustainer.cs:StartSample`(`sustainLoop`/`sustainLoopDurationRange`/`sustainIntervalRange` 决定循环与重播间隔)。
- `Verse/SoundDef.cs`:字段默认值(`maxVoices 4`/`maxSimultaneous 3`/`priorityMode PrioritizeNewest`)与 `ConfigErrors` 中 "sustainer 不能用 PrioritizeNewest";`Verse/Sound/SubSoundDef.cs:RandomizedVolume`(`volumeRange / 100`)。
- `Verse/Sound/SampleSustainer.cs:TryMakeAndPlay`(仅 `sustainLoop=true` 时 `source.loop = true`)、`SampleSustainer.Volume`(结束后按 `sustainFadeoutTime` 淡出、`sustainRelease` 处理样本尾音)。
- `Verse/Mote.cs`:`Alpha`(fadeInTime 淡入、`fadeOutUnmaintained` 按 fadeOutTime 淡出)、`TimeInterval`(`needsMaintenance` 停止续命后超时 `Destroy`)、`Maintain`、`Scale`/`linearScale`、`DrawPos`。
- `Verse/Graphic_Mote.cs:DrawMote`:实际使用 `mote.DrawPos`、`mote.Alpha`、`mote.exactRotation`、`ExactScale`(x/z 再乘 `graphicData.drawSize`)绘制。
- `Verse/Graphic_Mote.cs:DrawMote`(`matrix.SetTRS(mote.DrawPos, Quaternion.AngleAxis(mote.exactRotation, Vector3.up), exactScale)`)、`Verse/PawnRenderUtility.cs:DrawEquipmentAiming`(`Quaternion.AngleAxis(瞄准角 - 90 ± equippedAngleOffset, Vector3.up)`,朝南分支用 `plane10Flip`)、`Verse/PawnRenderUtility.cs:DrawEquipmentAndApparelExtras`(瞄准中武器落点 `drawPos + new Vector3(0,0,0.4 + def.equippedDistanceOffset).RotatedBy(瞄准角) * equipmentDrawDistanceFactor`)、`Verse/Vector3Utility.cs:AngleFlat`/`RotatedBy`、`Verse/Altitudes.cs:AltitudeFor`(`= 层号 x 0.36585`)。
- `Verse/Verb.cs:TryStartCastOn`(public virtual,建立 `Stance_Warmup` 的唯一入口)、`Verse/Stance_Warmup.cs` + `Verse/Stance_Busy.cs:verb`(判定"是否仍在瞄准")。
- `Verse/Verb.cs:468`(soundCast/soundCastTail 播放点)、`Verse/Sound/AudioGrain_Clip.cs`(clipPath → `ContentFinder<AudioClip>`)、`Verse/ContentFinder.cs`(AudioClip 走 mod 的 `Sounds/` 目录)、`Verse/ModAssetBundlesHandler.cs:16`(`AudioClipExtensions = .wav/.mp3/.ogg`)。

## 待确认

- 武器制作与外观取自参考文件 `D:\QQFL\鼠鼠收尾人们 副本\Defs\RK_fixers Weapon.xml`:手工制作点 + 大弓研究 + 6 级手工、60 木材、工作量 9000、重量 3.0、持握角度 165、交互音效 Bow_Large、`rotateInShelves=false`。
- 占位数值:死之箭重量 0.3 / 市场价 47、制作工作量 1000。
- 射击后冷却:未指定,当前 `RangedWeapon_Cooldown` 未设置(等于无冷却)。

## 踩坑记录(首次进游戏报出的配置错误)

- `ThingDef` 的 defName **不能以数字结尾**:`ThingDef.ConfigErrors` 对末位 `char.IsNumber` 报 "ends with a numerical digit, which is not allowed on ThingDefs."。基类 `Def.ConfigErrors` 只校验字符集 `^[a-zA-Z0-9\-_]*$`、不含此限制,只查基类会漏判。现有实例:`BowOfDeath_Arrow_Stage1~4` 改成 `..._StageOne/Two/Three/Four`;服装 `si1`/`si2` 改成 `si_uniform`/`si_quiver`。
- 带 `comps` 的 `HediffDef` 必须把 `hediffClass` 写成 `HediffWithComps` 或其子类,否则报 "has comps but hediffClass is not HediffWithComps or subclass thereof"(默认 hediffClass 是 `Hediff`)。意识增益 Hediff 因此显式加了 `HediffWithComps`。
- 跨引用不能指向抽象 Def:`RecipeDef.recipeUsers` 里写 `AnimalThingBase` 会报 "Could not resolve cross-reference"(抽象 Def 不参与跨引用解析)。改回原版豪猪棘刺的做法:手术配方不写 `recipeUsers`,用 `Patches/BowOfDeath_RecipeUsers.xml` 把配方名追加到种族 Def 的 `recipes` 列表;实定义用 `[defName="Human"]`,抽象定义必须用 `[@Name="AnimalThingBase"]`。
- `Graphic_StackCount` / `Graphic_Multi` 属于 `Graphic_Collection`:`Init` 用 `ContentFinder<Texture2D>.GetAllInFolder(req.path)` 把 `texPath` 当**文件夹**取贴图,按 `name.Split('_')[0]` 分组、`orderby name` 排序,因此该文件夹内不能混放其它物品的贴图(会被一并收进 `subGraphics`)。只放一张单图会报 `Collection cannot init: No textures found at path ...`。
- 死之箭已按堆叠实现:`Textures/Item/Bow_of_Death_Arrow/` 放 `Bow_of_Death_Arrow_a/_b/_c.png`,Def 使用 `Graphic_StackCount` + `texPath=Item/Bow_of_Death_Arrow`;`Graphic_StackCount.SubGraphicForStackCount` 在 3 张贴图时按 `count==1` / `count==stackLimit` / 其余取 a/b/c。投射物贴图与配方图标仍用 `Textures/Weapon/Bow_of_Death_Arrow.png`。
- 原版信息卡的远程条目(伤害/穿透/瞄准时间/射程/抑止/爆发)读的是 `ThingDef` 上那条**默认弹丸**与静态动词参数,不随运行时状态变化。精度四项例外:它走武器统计,组件 `GetStatOffset` 一改面板就跟着变。
- 信息卡组装顺序(`RimWorld/StatsReportUtility.cs` 的 `DrawStatsReport(Rect, Thing)`)是:先 `thing.def.SpecialDisplayStats(req)`,再 `StatsToDraw(thing)`(内含 `thing.SpecialDisplayStats()`)。所以**只靠武器实例类删不掉 Def 级条目**,只能追加;要替换必须补 `ThingDef.SpecialDisplayStats` 的 Harmony 后置补丁(`XIYUNTE.DeathBowStatPatches`),并限定 `req.Thing is Thing_DeathBow`,其他武器与其他 Def 浏览不受影响。
- 统计展开面板里"基础值 → 最终值"缺少来源行,是组件没有实现 `ThingComp.GetStatsExplanation`;补上后精度条目会显示"当前蓄力阶段: +16%"。
- 玩家据点地图上原版只允许"带 `orderedTakeGroup` 的物品"被拾取进背包(`PawnUtility.CanPickUp`:`!Map.IsPlayerHome` 才无条件允许,否则要求 `def.orderedTakeGroup.max > 0`)。自制弹药要能被捡起,需要自定义 `OrderedTakeGroupDef` 并挂到物品 Def 上;这与物品是否可搬运无关。
- 品质为大师级及以上时,"基础精度 + 蓄力阶加成"再乘品质系数会被 `Accuracy*` 的 `maxValue=1`(以及命中计算内的 clamp)截到 100%,高阶蓄力的精度收益在高品质武器上看不出来。要突破需要放宽该 StatDef 上限或改成对命中率乘算,会影响所有远程武器,需另行确认。
- 蓄力阶的介绍文字(`stages[].description`)、图标(`stages[].iconPath`)与按钮图标(`modeIconPath`)均为必填项,缺失由 `Properties_DeathBow.ConfigErrors` 在加载期报错;介绍文字是静态文本,改动弹丸数值时必须同步修改。
- 原版 `Graphic_StackCount` 在 3 张贴图时的取图规则是"`stackCount==1` 取 a、`stackCount==stackLimit` 取 c、其余取 b",即"单/中间量/满上限":c 只在**堆满一格**时出现(stackLimit=75 就要 75 支才触发,不是第 3 支触发);`SubGraphicForStackCount` 未标记 virtual,`MatSingle` 固定取最后一张。
- 原版没有"逐数量换图(1/2/3+)"的现成类:全部 `Graphic_*` 中与堆叠取图相关的只有 `Graphic_StackCount` 和食物专用的 `Graphic_MealVariants`,后者的每组 3 张处理与前者一致,并在超过 3 组时直接 `throw new NotImplementedException`。死之箭因此直接使用原版 `Graphic_StackCount`(纯 XML),不写自定义类。
- 堆叠物品的 UI 图标:`Widgets.GetIconFor` 的实例分支走 `thing.Graphic.MatAt(rot, thing)`,按当前 `stackCount` 取图,所以物品栏会显示"两支"贴图;给 Def 加 `uiIconPath` 指向单支贴图即可让物品栏、存储与交易界面统一显示 a(判定顺序 `Thing.UIIconOverride` → 样式图标 → `def.uiIconPath` → 实例 Graphic)。
- Hediff 在健康页的文字颜色来自 `Hediff.LabelColor`(默认返回 `HediffDef.defaultLabelColor`);意识提升因此设为四协会红 `(150,39,43)`(取自 `Textures/Clothing/si_uniform.png` 红色主色),死之箭 3 张堆叠贴图与弹道贴图的红色也映射到同一色值。
- 弹道贴图放大:投射物父级 `BaseBullet` 未定义 `drawSize`,在 4 个投射物 Def 的 `graphicData` 内加 `drawSize(2,2)` 即放大一倍,贴图文件本身保持 256x256。
