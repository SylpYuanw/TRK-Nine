# 鼠鼠收尾人：发布与仓库信息

本文件记录发布相关的维护信息。README 面向公开发布,只写项目说明与仓库结构;变更记录写在 git 提交信息里,本机路径与发布步骤记录在此。

## 仓库

- 发布仓库:`https://github.com/SylpYuanw/TRK-Nine`(分支 `main`)
- 本地克隆:`C:\Users\Administrator\Desktop\天翼种\TRK-Nine`
- 源工作区:`C:\Users\Administrator\Desktop\天翼种\鼠鼠收尾人们目前已到4579协会`
- 仓库结构:MOD 本体位于仓库 `鼠鼠收尾人们目前已到4579协会/`;源码位于仓库根 `Source/`

## 发布流程

1. 编译:`MSBuild Source\ClassLibrary1\ClassLibrary1.csproj -p:Configuration=Release`,输出即 MOD 的 `Assemblies/ClassLibrary1.dll`。
2. 同步:工作区 MOD 本体文件 → 仓库 `鼠鼠收尾人们目前已到4579协会/`;工作区 `Source/` → 仓库 `Source/`。
3. 推送:`git add -A` → `git commit -m "<变更说明>"` → `git push origin main`(凭据由 Windows credential manager 保存,可直接推送);提交信息按下方规范写。
4. 提交推送前无需向用户确认仓库地址与目录对应关系;仅在推送失败或改动范围超出本项目时询问。

## 提交信息规范

- 变更记录写在 git commit message 里,不写进 README;README 是要上传的公开文件,只写项目说明与仓库结构。
- 提交信息中英对照:先写完中文内容,空两行,再写英文内容。
- 中英文两部分各自列出关联文件(源码、程序集等)。
- 提交信息同样不写本机路径、编译命令、同步规则、推送步骤。
